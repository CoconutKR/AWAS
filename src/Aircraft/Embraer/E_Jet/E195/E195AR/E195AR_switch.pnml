/*--------------------------------------------------------------------
스위치
--------------------------------------------------------------------*/
switch(FEAT_AIRCRAFT, SELF, sw_Embraer_E195AR_cargo_subtype_text, cargo_subtype) {
    1: return string(STR_REFIT_LIVERY_Embraer);   // Manufacturer
    2: return string(STR_REFIT_LIVERY_Embraer);     // Manufacturer
    return string(STR_REFIT_LIVERY_Embraer);
}

switch(FEAT_AIRCRAFT, SELF, sw_E195AR, cargo_subtype) {
    1: set_E195AR_default;        // Manufacturer
    2: set_E195AR_default; 
    set_E195AR_default;
}

